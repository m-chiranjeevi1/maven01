//def githubcred = 'github_token'
pipeline {
agent any
options {
overrideIndexTriggers(false)
disableConcurrentBuilds()
timestamps()
ansiColor('xterm')
}
  stages {
    stage('hello') {
       steps {
         echo "hello world!"
       }
    }
    stage('build') {
      steps {
       sh 'mvn clean install'
      }
    }
  }
}
